# SafeFam 성능 검증 보고서

## 0. 요약

- SafeFam 전체 파이프라인은 400건(사기 200 / 정상 200, 학습 데이터와 겹치지 않는 클린 표본) 기준 **탐지율 98.0%, 오탐률 22.0%**를 기록했습니다.
- 규칙 엔진 단독(2~4% 탐지율)이나 LLM 단독(오탐률 45%)보다, 나이브베이즈 → Gemini로 이어지는 하이브리드 구조가 탐지율과 오탐률 양쪽에서 더 우수합니다.
- 사기문자 변형 공격 10종(규칙기반 6종 + Gemini 패러프레이즈 4종) 실험에서, 규칙 엔진은 변형 시 탐지율이 57.2%까지 떨어지지만 SafeFam 전체 파이프라인은 **98.4%**를 유지했습니다. 테스트 도중 "문체를 정상 공지문처럼 바꾸는" 변형에서 파이프라인 탐지율이 16.0%까지 떨어지는 실제 취약점을 발견했고, 근본 원인(나이브베이즈가 Gemini 2차 검증을 조기에 건너뛰는 로직)을 찾아 **당일 수정 및 재검증까지 완료**했습니다 (§2.4).

---

## 1. 성능 벤치마크

### 1.1 방법론

- **데이터**: `data_science/Data/SMSData/phishing_total_dataset_2705.csv` (실제 신고 사례 기반, phishing 1,905건 / normal 1,025건)
- **표본**: 사기 200건 / 정상 200건, 라벨 내 유형(type)별 층화추출
- **⚠️ 데이터 오염 검증 및 제거**: 나이브베이즈 모델은 이 데이터셋에서 학습(571)/검증(123)/테스트(123)로 미리 분리되어 학습됐습니다(`data_science/SMSModel/splits/sms_split_v1.csv`). 최초 벤치마크는 이 분리를 무시하고 전체 데이터에서 무작위 추출해 진행했는데, 검증 결과 표본의 31%가 학습 데이터와 정규화 후 완전히 동일한 문장이었습니다. 이후 학습에 사용된 문장을 전부 제외한 **클린 표본으로 재검증**했으며, 아래 수치는 모두 클린 표본 기준입니다. (오염 제거 전후 수치 차이는 1%p 이내로, 모델이 암기가 아닌 실제 일반화를 하고 있음을 확인했습니다.)
- **탐지 기준**: 위험도 점수 40점 이상(SUSPICIOUS/MEDIUM 이상)을 탐지로 판정 (SafeFam 등급 정책과 동일 기준)
- **모델 표기**: 규칙엔진/나이브베이즈는 프로덕션 코드를 그대로 호출한 실측치입니다. LLM 관련 트랙(LLM만/NB+LLM/SafeFam 전체)은 Gemini 무료 티어의 프로덕션 기본 모델(`gemini-flash-latest`) 일일 호출 한도(20건) 제약으로 인해 **`gemini-3.1-flash-lite`**로 측정했습니다(같은 계열의 경량 모델, 일일 한도 500건).

### 1.2 SafeFam 전체 파이프라인 (핵심 수치)

```
총 400건 평가
사기 문자 200건 중 196건 탐지
탐지율 98.0%
정상 문자 200건 중 44건 오탐
정상 문자 오탐률 22.0%
평균 분석 시간 0.97초
```

### 1.3 트랙별 비교

| 트랙 | 탐지율(Recall) | 오탐률(FPR) | F1 | 평균 분석시간 |
| --- | ---: | ---: | ---: | ---: |
| 규칙만 | 4.0% | 15.0% | 0.067 | 0ms |
| 나이브베이즈(NB)만 | 98.0% | 22.0% | 0.891 | 2ms |
| LLM만 (`gemini-3.1-flash-lite`) | 100.0% | 45.0% | 0.816 | 1615ms |
| NB + LLM 하이브리드 | 98.0% | 22.0% | 0.891 | 971ms |
| **SafeFam 전체** (규칙+NB+LLM+URL) | **98.0%** | **22.0%** | **0.891** | **971ms** |

### 1.4 하이브리드 구조가 필요한 이유

- **LLM 단독은 오탐률이 가장 높습니다(45%)**. 재현율은 100%로 가장 높지만 정상 문자의 절반 가까이를 사기로 오판합니다.
- **나이브베이즈를 1차 필터로 두면 오탐률이 45%→22.0%로 낮아지고, 속도도 개선됩니다.** 정상 문자의 상당수(160/200)를 나이브베이즈가 "SAFE"로 확신해 Gemini 호출 자체를 생략하기 때문에, 평균 분석시간이 LLM 단독 1615ms → 하이브리드 971ms로 40% 단축됩니다.
- **SafeFam 전체 = NB+LLM 하이브리드와 동일한 수치**입니다. 이는 URL 트랙이 이번 데이터셋(오래된 신고 사례라 링크 대부분 만료됨)에서 악성 URL을 하나도 탐지하지 못해 추가 기여가 없었기 때문입니다. 최신 유포 중인 악성 URL이 포함된 표본에서는 URL 트랙의 기여도가 별도로 나타날 것으로 예상됩니다.

### 1.5 발견된 나이브베이즈 약점

정상 문자 오탐 44건 중 상당수가 특정 템플릿(정상 포인트 소멸 안내 등 "이달 말 소멸 예정"류 안내문)에 집중되어 있습니다. 긴급성을 연상시키는 정상 안내 문구를 사기 패턴으로 오인식하는 경향이 있어, 후속 모델 개선 시 우선순위로 고려할 만합니다.

---

## 2. 변형 공격(적대적 강건성) 테스트

### 2.1 방법론

- 규칙 엔진이 이미 탐지하는(rule_score ≥ 40) 금융기관사칭형 사기문자 원본 25건을 선정
- 각 원본에 10종 변형을 적용: 띄어쓰기 삽입, 특수문자 삽입, 기관명 변형, 숫자→문자 치환, URL 제거, 오탈자 삽입 (규칙기반 6종, 정규식/치환) / 긴급성 순화, 문체 정상화, 축약, 전화상담 유도 (Gemini 패러프레이즈 4종, 의미보존 재작성)
- 원본·변형(총 275건) 각각을 규칙엔진 단독 vs SafeFam 전체 파이프라인으로 평가해 탐지율 비교
- **모델 표기**: Gemini 무료 티어 일일 한도 제약으로, 파이프라인 평가에 프로덕션 기본 모델(`gemini-flash-latest`) 대신 경량 대체 모델을 사용했습니다. 한도가 순차적으로 소진되면서 `gemini-3.1-flash-lite`(176건) → `gemini-3.5-flash`(14건) → `gemini-3-flash-preview`(13건) → `gemini-3.5-flash-lite`(72건) 4개 모델에 걸쳐 나뉘어 측정됐습니다. 전부 프로덕션 기본 모델과 다른 대체 모델입니다.

### 2.2 결과

| 구분 | 건수 | 규칙엔진 단독 탐지율 | SafeFam 전체 탐지율 |
| --- | ---: | ---: | ---: |
| 원본 | 25 | 100.0% | 100.0% |
| 변형 전체 | 250 | 57.2% | 98.4% |

275건(원본 25 + 변형 250) 전체 실측 완료. §2.4에서 설명하는 하이브리드 로직 수정 전에는 88.8%였으며, 수정 후 재검증한 수치입니다.

### 2.3 변형 유형별 탐지율

| 변형 유형 | 규칙엔진 탐지율 | SafeFam 전체 탐지율 |
| --- | ---: | ---: |
| institution_name_variation (기관명 변형) | 0.0% | 100.0% |
| spacing_insertion (띄어쓰기 삽입) | 24.0% | 100.0% |
| special_char_insertion (특수문자 삽입) | 32.0% | 96.0% |
| shortening (축약) | 32.0% | 96.0% |
| typo_injection (오탈자) | 44.0% | 96.0% |
| digit_to_word_substitution (숫자→문자) | 48.0% | 100.0% |
| phone_call_redirect (전화상담 유도) | 96.0% | 100.0% |
| tone_normalization (문체 정상화) | 96.0% | 96.0% |
| url_removal (URL 제거) | 100.0% | 100.0% |
| urgency_softening (긴급성 순화) | 100.0% | 100.0% |

### 2.4 발견한 취약점과 당일 수정: "정상 공지문처럼 문체를 바꾸는" 변형

**최초 발견**: 규칙기반 변형 6종은 SafeFam 전체가 대부분 96~100% 탐지하지만, Gemini로 문체만 격식 있는 공지문처럼 재작성한 `tone_normalization`은 파이프라인 탐지율이 **16.0%**로 급락했습니다(25건 중 4건만 탐지). 같은 계열인 `urgency_softening`(긴급 표현 순화)도 84.0%로 낮았습니다.

실패 사례 (규칙점수 55점인데도 파이프라인 24점 LOW로 미탐지):

```
[KB국민은행] 결제 완료 안내

000 고객님, 요청하신 위닉스 세탁기 구매 건에 대해 KB국민은행을 통한
676원의 결제가 정상적으로 완료되었습니다.

본인이 결제하지 않으셨거나 명의 도용 등의 금융 사고가 의심되는 경우,
즉시 아래의 고객센터로 연락하여 조치를 취하시기 바랍니다.

■ 신고 및 문의처: 02-330-4284
```

**근본 원인 진단**: 로그를 직접 추적한 결과, Gemini 자체가 속은 게 아니라 **Gemini가 아예 호출되지 않은 것**이 원인이었습니다. SafeFam의 텍스트 트랙은 "1차 나이브베이즈가 SAFE로 판정하면 2차 Gemini 검증을 생략"하는 하이브리드 구조인데, 정중한 공지문 형식의 어휘가 나이브베이즈를 속여 SAFE(위험도 7~21점)로 오판시켰고, 그 순간 Gemini는 실행 기회조차 얻지 못했습니다. 규칙엔진은 계좌번호·기관명 패턴으로 45~55점을 매겼는데도, 나이브베이즈의 단독 SAFE 판정만으로 전체 검증이 조기 종료된 것입니다.

**수정**: `app/analysis/service.py`의 스킵 조건에 규칙엔진 점수를 반영해, 규칙엔진이 이미 SUSPICIOUS 이상(rule_score ≥ 40)을 감지한 경우에는 나이브베이즈의 SAFE 판정만으로 Gemini 검증을 생략하지 않도록 수정했습니다. Gemini 자체의 판단 프롬프트에도 "정중한 어조는 안전 신호가 아니다"라는 명시적 지침과 few-shot 예시를 추가했습니다.

**재검증 결과**: 기존 실패 사례(tone_normalization 21건, urgency_softening 4건) 전체를 실제 API로 재평가한 결과, **tone_normalization 21건 중 20건, urgency_softening 4건 전체가 탐지로 전환**됐습니다. 두 유형 모두 96~100%로 개선되어, 위 §2.2/§2.3 수치는 이 수정 반영 후의 최종 값입니다. 남은 1건(규칙점수 30점, 기관명 미등록으로 규칙엔진 임계값 미달)은 잔여 사각지대로 남아있습니다.

반대로, 띄어쓰기·특수문자·기관명 표기를 바꾸는 표면적 변형(institution_name_variation 등)에는 SafeFam 전체가 규칙엔진 우회 여부와 무관하게 잘 대응합니다. 변형 250건 중 104건(41.6%)이 "규칙엔진은 우회했지만 SafeFam 전체는 탐지"한 사례입니다.

### 2.5 암기 여부 검증

원본 25건 중 8건(32%)은 나이브베이즈 학습셋과 겹치는 문장이었습니다. 수정 후 재검증 기준으로, 학습에 전혀 쓰이지 않은 "깨끗한" 원본에서 나온 변형(160건)의 파이프라인 탐지율이 99.4%로, 오염된 원본에서 나온 변형(96.2%)보다 오히려 높아, §2.4의 개선이 암기가 아닌 실제 구조적 수정(하이브리드 스킵 로직 보완)에 의한 것으로 확인됩니다.

### 2.6 한계

1. **4개의 서로 다른 대체 모델이 섞여 있습니다** (§2.1). 프로덕션 모델 하나로 통일된 검증이 아니므로, 모델 간 판정 성향 차이가 수치에 일부 반영됐을 수 있습니다. §2.4의 재검증은 `gemini-3.5-flash-lite` 단일 모델로 통일해 진행했습니다.
2. **§2.4에서 적용한 수정은 유료 티어로 전환해 프로덕션 모델(`gemini-flash-latest`)로도 재확인이 필요합니다.** 대체 모델(경량 lite 계열)에서는 효과가 확인됐으나, 프로덕션 모델에서 동일하게 작동하는지는 별도 검증 대상입니다.
3. 규칙엔진 임계값(rule_score < 40)에 걸리지 않는 매우 약한 신호의 문체 위장 공격은 여전히 나이브베이즈 단독 판정에 의존합니다(§2.4의 잔여 사각지대 1건이 이 경우). 임계값을 더 낮추면 이런 사각지대는 줄지만 Gemini 호출량(비용)이 늘어나는 트레이드오프가 있습니다.
4. Gemini 패러프레이즈로 생성된 변형문 자체가 특정 스타일(공손체, "고객센터"/"조치를 취하시기 바랍니다" 등 상투구)로 수렴하는 경향이 있어, 실제 공격자가 만드는 변형과 표현이 다를 수 있습니다.

---

## 3. 기존 스미싱 차단 서비스와의 차이

| 기존 방식 | SafeFam |
| --- | --- |
| 알려진 번호·URL 블랙리스트 중심 차단 | 규칙(계좌/기관명 패턴) + URL 안전 브라우징(GSB/VT) + 문맥분석(나이브베이즈→Gemini 하이브리드)을 병렬 실행 후 3중 가중합산 |
| 위험 여부만 O/X로 표시 | `tone_analysis`·`evidence`·`reason` 필드로 판단 근거를 사용자 언어로 설명 (원문 인용 없이 AI가 생성) |
| 위험 판정만 하고 끝 | 챗봇으로 경찰청·KISA·금융감독원 등 신고기관 상담까지 자동 연계 |

---

## 4. 종합 한계 및 후속 작업

1. **LLM 관련 수치는 프로덕션 기본 모델이 아닌 대체 모델(성능 벤치마크는 `gemini-3.1-flash-lite`, 변형 공격 테스트는 4개 모델 혼합) 기준입니다.** 유료 티어 전환 시 프로덕션 모델(`gemini-flash-latest`)로 재검증 필요.
2. **URL 트랙은 오래된 데이터셋(만료된 링크) 특성상 이번 벤치마크에서 기여도가 0으로 측정됐습니다.** 최신 악성 URL 표본으로 별도 검증 권장.
3. **"정상 공지문처럼 문체를 바꾸는" 변형(tone_normalization)의 취약점을 발견해 당일 수정 및 재검증까지 완료했습니다 (§2.4).** 근본 원인(나이브베이즈 SAFE 판정 시 Gemini 2차 검증 스킵)을 `app/analysis/service.py`에서 수정했으며, 프로덕션 모델(`gemini-flash-latest`)로 최종 재확인은 유료 티어 전환 후 진행 필요합니다.
4. **VirusTotal API는 무료 티어 한도(하루 약 70~80건 수준으로 추정)를 벤치마크 도중 초과했습니다.** GSB 우선 판정으로 결과 자체는 안전하게 유지됐으나, 대규모 URL 검증 시 유료 전환 또는 호출량 관리가 필요합니다.
