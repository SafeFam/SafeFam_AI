[pytest]
markers =
    integration: hits real external hosts over the network; excluded by default, run explicitly with -m integration
    slow: performs relatively expensive local data processing
    asyncio: mark test as async
addopts = -m "not integration"
asyncio_mode = auto