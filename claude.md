# 음성 피싱 탐지 데이터셋 — 전처리 및 ML 컬럼 활용 계획

> 대상 파일: `metadata.csv` (= `metadata.jsonl`, 동일 내용)
> 총 3,000행 / phishing 1,500 · normal 1,500 / train 2,100 · val 450 · test 450

---

## 0. 최우선 이슈: 전사(transcription) 표기 방식에 의한 데이터 누수

phishing(FSS)과 normal(AI Hub)은 서로 다른 전사 규칙을 사용했고, 이 규칙 차이가 `label`과 **거의 1:1로 대응**합니다.

| 패턴 | normal(n=1500) | phishing(n=1500) | 의미 |
|---|---|---|---|
| `n/`, `아/`, `b/`, `씁/` 등 발화자·간투사 태그 | 1,268건 | 0건 | AI Hub 전사 컨벤션 |
| `(80)/(팔순)` 형태 숫자 발음 주석 | 894건 | 0건 | AI Hub 전사 컨벤션 |
| `+` 단어 정정 표시 | 336건 | 0건 | AI Hub 전사 컨벤션 |
| `...` 말줄임 | 0건 | 97건 | FSS 전사 컨벤션 |

**이 패턴들을 그대로 두고 학습하면 모델은 "피싱 여부"가 아니라 "어느 기관이 전사했는가"를 학습합니다.** 검증 지표는 비정상적으로 높게 나오지만 실제 서비스 입력(신고 문자, 실시간 STT 결과)에는 전혀 일반화되지 않습니다.

**대응**: 벡터화 이전에 위 태그류를 전부 정규화/제거하는 정제 단계를 필수로 거칠 것 (2-2절 참조).

---

## 1. 컬럼별 용도 분류

| 컬럼 | 역할 | 사용 여부 |
|---|---|---|
| `sample_id` | 고유 식별자 | 학습에 미사용, 결과 추적용 보관 |
| `split` | train/validation/test 지정 | **분할 기준으로 사용** (재분할 금지) |
| `label` | phishing/normal | 사람이 읽는 라벨, EDA·리포트용 |
| `label_id` | 1/0 | **타겟 변수(y)** |
| `parent_call_id` | 원본 통화 단위 ID | 미사용(이미 분할 시 그룹 기준으로 반영됨), 검증용으로만 보관 |
| `source_id` | 세부 소스 ID | 미사용, 추적용 |
| `source_dataset` | fss/aihub | **학습 피처로 사용 금지** — label과 완전 대응(0번 이슈)이라 그대로 넣으면 트리비얼 리키지. EDA·정제 로직 분기에만 사용 |
| `source_audio_path` | 로컬 원본 경로(Windows 절대경로) | 미사용, 개인 PC 경로라 오히려 마스킹 대상 |
| `audio_path` | 상대 wav 경로 | 텍스트 모델 단계에서는 미사용. 향후 오디오 임베딩 단계에서 사용 |
| `start_sec`, `end_sec` | 원본 통화 내 구간 위치 | 미사용 (모델 피처로 의미 없음) |
| `duration_sec` | 세그먼트 길이(10~30초) | **선택적 구조적 피처**(발화 속도 등 파생 가능) — 단, 분포 차이가 label과 무관한지 먼저 확인 |
| `text` | 전사문 | **핵심 피처(X)의 원천** |
| `qa_status` | PASS(phishing) / SOURCE_TEXT(normal) | **학습 피처로 사용 금지** — label과 완전 대응(값 자체가 사실상 라벨). 필터링에만 사용했던 컬럼이라 이미 목적을 다함 |
| `qa_flags` | 전부 공백(최종본은 REVIEW 제외됨) | 미사용, 정보 없음 |
| `is_augmented` | 전부 False | 미사용 |
| `augmentation` | 전부 공백 | 미사용 |

**결론**: 실질적으로 모델에 들어가는 것은 `text` 하나이고, 나머지는 분할·검증·추적용 메타데이터입니다. 이 데이터셋은 SMS 프로젝트와 달리 **URL/전화번호/계좌번호 같은 구조적 신호가 텍스트 안에 거의 없는 대화체 통화 전사문**이라는 점이 근본적으로 다릅니다 → 구조적 boolean 피처(`has_url` 등) 전략은 여기서는 효과가 제한적일 가능성이 높습니다.

---

## 2. 전처리 파이프라인

### 2-1. 로딩
- CSV는 한글 텍스트에 쉼표·따옴표가 섞여 있어 파싱 오류 위험이 있으므로 **`metadata.jsonl`을 우선 사용** 권장 (라인 단위 JSON이라 이스케이핑 문제 없음). CSV를 쓴다면 반드시 `encoding='utf-8-sig'`.

### 2-2. 텍스트 정제 (0번 이슈 해결 — 필수)
아래 순서로 `text`를 정규화한 `text_clean` 컬럼 생성:

1. **AI Hub 숫자 주석 제거**: `(80)/(팔순)` → `팔순` (슬래시 뒤 실제 발화형만 채택. 정규식: `\(([^)]*)\)/\(([^)]*)\)` → 두 번째 그룹 사용)
2. **발화 태그 제거**: `n/`, `아/`, `b/`, `씁/` 등 "한글 또는 알파벳 1~2자 + `/`" 패턴 제거 (단어 경계·문장 시작 위치 기준으로 오탐 주의 — 특히 정상 대화 중 실제 단어에 `/`가 우연히 붙는 경우는 거의 없어 안전)
3. **단어 정정 `+` 병합**: `할아버+ 할머니께서` 같은 자기 정정 패턴은 정정 후 표현만 남기거나, 혹은 반대로 정정 자체를 "발화 반복" 구조적 피처로 별도 추출 (판단 필요 — 2-4절 참고)
4. **말줄임 `...` 처리**: 제거하지 않고 `<PAUSE>` 같은 공통 토큰으로 정규화하거나 완전 제거 — FSS/AI Hub 양쪽에 동일 처리 규칙 적용
5. **공백/구두점 정리**: 중복 공백 제거, 문장부호 정규화

정제 후 **반드시 재검증**: normal/phishing 각각에서 위 4개 패턴 발생률이 0에 가깝게 수렴하는지 확인. 남아있다면 정규식 보완.

### 2-3. 분할 재사용
- `split` 컬럼과 `parent_call_id` 기준 그룹 분할이 이미 완료·검증됨(`validation_report.json`: `parent_split_leakage_count: 0`). **재분할하지 말고 그대로 사용**.
- 단, 정제 후에도 동일 `parent_call_id`의 세그먼트들이 서로 다른 split에 섞이지 않았는지 한 번 더 자체 확인 권장 (외부 파일 신뢰만으로 넘어가지 않기).

### 2-4. 구조적 피처 (선택, 효과 검증 필요)
SMS 프로젝트의 `has_url`/`has_phone`류는 이 통화 전사문에는 거의 해당 없음. 대신 시도해볼 만한 것:
- `text_length` (정제 후 글자 수)
- `duration_sec` (단, source_dataset과 분포가 겹치는지 먼저 t-test/분포 시각화로 확인 — 다르면 이 역시 리키지 후보)
- `question_mark_count`, `exclamation_count` (긴급성 유도 여부의 약한 프록시)
- 금전/은행/입금 관련 키워드 존재 여부 (도메인 특화 사전 기반 boolean) — 이건 실제 피싱 신호일 가능성이 있어 시도 가치 있음

이 피처들은 반드시 **정제된 `text_clean` 기준**으로 만들 것 (원본 `text`로 만들면 태그 패턴이 다시 새어 들어감).

### 2-5. 벡터화
- 기존 SMS 프로젝트와 동일하게 `TfidfVectorizer(analyzer='char_wb', ngram_range=(2,4))` 우선 적용 (한국어 조사 변이에 강건).
- 다만 이 데이터는 **문장 하나가 아니라 여러 발화 턴이 이어진 대화 세그먼트**이므로, char n-gram이 다수 화자의 말투 차이(존댓말/반말, 상담원 특유 어투)까지 과하게 학습할 위험이 있음 → 학습 후 반드시 SHAP/feature importance로 상위 n-gram을 사람이 직접 확인해서 "진짜 피싱 신호"인지 "화자 말투 아티팩트"인지 판별할 것.

### 2-6. 분류기
- Baseline: `MultinomialNB` + `CountVectorizer` (기존 계획과 동일)
- 비교군: LightGBM, Linear SVM, Logistic Regression
- 평가: Recall 우선 (기존 원칙 유지)

---

## 3. 검증 체크리스트 (모델 학습 전 필수 확인)

- [ ] `text_clean`에서 태그 패턴(`n/`, `(x)/(y)`, `+`, `...`) 잔존 여부가 normal/phishing 양쪽에서 0에 수렴
- [ ] `duration_sec` 분포가 label 간 유의미하게 다르지 않은지 확인 (다르면 피처 제외 또는 별도 논의)
- [ ] `source_dataset`, `qa_status`는 어떤 피처 파이프라인에도 포함되지 않았는지 코드 리뷰로 재확인
- [ ] baseline 모델 학습 후, 상위 중요 피처(SHAP)를 사람이 직접 훑어서 "화자 어투/전사 관습" 신호가 섞여있지 않은지 확인 — 섞여 있으면 2-2 정제 로직을 보강하고 재학습

---

## 4. 다음 단계 제안

1. 위 정제 로직을 스크립트로 구현 (`preprocess_voice_metadata.py`)
2. 정제 전/후 baseline 성능 비교 — **정제 전 성능이 비정상적으로 높다면 0번 이슈가 실제로 작동했다는 증거**이므로 이 비교 자체가 좋은 검증 근거가 됨
3. SMS 텍스트 프로젝트와 이 통화 전사 프로젝트를 같은 파이프라인으로 통합할지, 별도 모델로 둘지 결정 필요 (도메인이 다름: 짧은 단문 vs 긴 대화 세그먼트)
